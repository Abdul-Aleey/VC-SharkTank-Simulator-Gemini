{
  "name": "vc-shark-tank-simulator",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend": "cd backend && uvicorn server:app --host 127.0.0.1 --port 5000"
  }
}
